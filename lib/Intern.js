class Intern {
  constructor(name, id, email, school) {
    (this.name = name),
      (this.id = id),
      (this.email = email),
      (this.school = school),
      (this.role = 'intern');
  }
}

module.exports = Intern;
